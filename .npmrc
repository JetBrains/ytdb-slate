# Do not install the "*" SDK peers from the registry. Tests link the exact peers
# bundled with the user's pi install; unlike legacy-peer-deps, this keeps npm's
# peer-conflict checks active.
omit=peer
